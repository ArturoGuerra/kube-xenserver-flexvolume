apiVersion: v1
kind: Pod
metadata:
  name: test
  namespace: testing
spec:
  volumes:
    - name: config
      persistentVolumeClaim:
        claimName: xenserver-test-claim
  containers:
    - name: test
      image: nginx
      volumeMounts:
        - name: config
          mountPath: /usr/share/nginx/html
