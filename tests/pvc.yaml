apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xenserver-test-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage:
        1Gi
  storageClassName: ""
  volumeName: xenserver-test-claim
